#include <stdio.h>

int main() {
    /* Parse commandline args */

    /* File opening */

    /* Count occurrences of each character */

    /* Compile counts into a list */

    /* Build Huffman tree */

    /* Output into file */

    return 0;
}
